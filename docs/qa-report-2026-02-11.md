# RoastMySite QA Report (2026-02-11)

## Scope
- Automated QA setup and execution for API/auth/rate-limit critical paths.
- Regression tests added for v1.2 tiering + waitlist behavior.
- Build/lint/test pipeline executed locally.

## Automated Test Results
- Total automated tests: `24`
- Passed: `24`
- Failed: `0`
- Command: `npm run test:qa`

### Added Test Coverage
1. URL validation and security blocking
2. Rate limit logic (free/waitlist/pro + IST day key reset)
3. Clerk token verification + metadata-derived status
4. `/api/roast` guard paths:
   - Unauthorized (`401`)
   - Waitlist pending (`403`)
   - Free tier limit reached (`429`)
5. `/api/account`, `/api/history`, `/api/pro-waitlist` authenticated/unauthenticated behavior
6. `analyzeWebsite` metrics-source behavior:
   - PageSpeed 429 fallback to `estimated`
   - Live PageSpeed response maps to `pagespeed`

## Bugs Found and Fixed
1. **Automated tests could not import API handlers under Node test runner**
   - Root cause: API files use extensionless internal imports (works in Vite SSR, fails in raw Node ESM resolution).
   - Fix: Added test-only ESM loader to resolve `./module` to `./module.ts` during tests.
   - Files:
     - `tests/ts-extension-loader.mjs`
     - `package.json` (`test` script updated)
   - Impact: Unblocks handler-level regression tests without modifying production API imports.

## Build and Quality Checks
- Lint: `npm run lint` passed
- Build: `npm run build` passed
- Bundle (from latest build):
  - JS: `492.12 kB` raw, `147.98 kB` gzip
  - CSS: `41.21 kB` raw, `8.19 kB` gzip

## Security/Code Hygiene Checks
- `TODO/FIXME` in `src/`, `api/`, `docs/`: none found
- Hardcoded secret patterns in code: none found
- `console.log` usage: found only in local dev API simulator (`vite.config.ts`)

## Manual / Live Tests Status
The following require browser-driven or production-environment execution and were **not fully executable** inside this local CLI-only run:
1. Clerk UI flows (Google OAuth screens, modal rendering)
2. Mobile touch UX checks (iPhone/iPad physical viewport interaction)
3. Multi-browser visual checks (Chrome/Firefox/Safari/Edge manual)
4. Live external dependency behavior under real network load:
   - Groq credits dashboard parity
   - Firecrawl credit dashboard parity
   - Vercel production route latency under load
5. Production smoke on `roastmy.site` domain and OG preview rendering

## Remaining QA Gaps
1. Full E2E browser journey (Playwright/Cypress) is still pending.
2. Load test profile (`100 concurrent`, `1000/hr`) is still pending.
3. Lighthouse production benchmark target validation (`Perf >= 90`) still pending.
4. True p50/p95/p99 API latency from production telemetry still pending.

## Recommendation
- Automated regression baseline is now stable and passing.
- Run browser E2E + production smoke checklist before launch window.
